# 🤖 ربات تلگرام استخراج و ارسال کانفیگ V2Ray

رباتی برای استخراج خودکار کانفیگ‌های VPN از منابع مختلف و ارسال به کانال تلگرام.

---

## ✨ ویژگی‌ها

### استخراج کانفیگ
- **۷ پروتکل**: VLESS, VMess, Trojan, Shadowsocks, SSR, TUIC, Hysteria2
- **۴ منبع**: فایل HTML، متن ساده، لینک Subscription، فایل (TXT/JSON/YAML)

### تشخیص هوشمند کشور
- از متن پست (نام فارسی/انگلیسی، کد کشور، نام شهر)
- از ریمارک کانفیگ
- از IP سرور با GeoIP API

### مدیریت کانفیگ‌ها
- جلوگیری از ارسال تکراری (هش یکتا + IP)
- حذف خودکار کانفیگ‌های قدیمی‌تر از ۱۰ روز
- ریمارک اختصاصی روی هر کانفیگ: `@BotUsername_COUNTRY`

### رابط کاربری
- کیبورد فارسی دو ستونی
- آمار کامل و تفکیکی
- پینگ نمایشی با کیفیت

---

## 📁 ساختار فایل‌ها

```
src/
├── app/
│   ├── api/bot/route.ts       ← Webhook endpoint
│   └── page.tsx               ← صفحه وب
│
├── lib/
│   ├── db.ts                  ← دیتابیس JSON
│   │
│   ├── bot/
│   │   └── index.ts           ← ربات تلگرام
│   │
│   ├── parser/
│   │   ├── index.ts           ← پارسر اصلی
│   │   ├── types.ts           ← تایپ‌ها و کشورها
│   │   ├── vless.ts           ← پارسر VLESS
│   │   ├── vmess.ts           ← پارسر VMess
│   │   ├── trojan.ts          ← پارسر Trojan
│   │   ├── shadowsocks.ts     ← پارسر SS
│   │   ├── ssr.ts             ← پارسر SSR
│   │   ├── tuic.ts            ← پارسر TUIC
│   │   └── hysteria2.ts       ← پارسر Hysteria2
│   │
│   └── utils/
│       ├── geoip.ts           ← تشخیص کشور
│       └── remark.ts          ← ریمارک و فرمت‌بندی
│
└── db/
    └── bot-db.json            ← فایل دیتابیس
```

---

## 🚀 راه‌اندازی

### ۱. پیشنیازها

| مورد | نحوه دریافت |
|------|-------------|
| **BOT_TOKEN** | از @BotFather → /newbot |
| **ADMIN_ID** | از @userinfobot |
| **CHANNEL_ID** | فوروارد پیام کانال به @userinfobot |

### ۲. تنظیم متغیرها

در Railway یا فایل `.env.local`:

```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxYZ
ADMIN_ID=123456789
CHANNEL_ID=-1001234567890
BOT_USERNAME=nonecorebot
```

### ۳. دیپلوی روی Railway

```
1. پروژه را به Railway وصل کنید
2. متغیرهای محیطی را تنظیم کنید
3. Deploy کنید
```

### ۴. تنظیم Webhook

به آدرس زیر بروید:
```
https://your-app.up.railway.app/api/bot?action=set
```

### ۵. تنظیم کانال

```
1. ربات را به کانال اضافه کنید
2. ربات را ادمین کنید
3. دسترسی Post Messages بدهید
```

---

## 📱 استفاده

### کیبورد اصلی

| دکمه | عملکرد |
|------|--------|
| 📤 آپلود HTML | ارسال فایل یا متن حاوی کانفیگ |
| 🔗 لینک ساب | ارسال لینک Subscription |
| 📤 ارسال دستی | ارسال تعداد مشخصی کانفیگ |
| 📊 آمار | مشاهده آمار ربات |
| ⚙️ تنظیمات | مشاهده تنظیمات |
| 🗑️ پاک کردن | ریست دیتابیس |

---

## 📝 نمونه پست کانال

```
📍 آلمان DE 📶 ۴۵ms (عالی)

[کانفیگ در spoiler]

🏷️ ریمارک: @nonecorebot_DE

#VLESS #فیلترشکن #VPN #آلمان

━━━━━━━━━━━━━━━━━
✅ فعال | ⚠️ فقط برای وب‌گردی
📱 کلاینت‌ها: v2rayNG, Hiddify, Streisand

@nonecorebot
```

---

## 🔧 API Endpoints

| Endpoint | عملکرد |
|----------|--------|
| `GET /api/bot` | اطلاعات endpoint |
| `GET /api/bot?action=set` | تنظیم webhook |
| `GET /api/bot?action=info` | اطلاعات webhook |
| `GET /api/bot?action=delete` | حذف webhook |
| `POST /api/bot` | دریافت آپدیت تلگرام |

---

## 🌐 GeoIP API

| API | محدودیت |
|-----|---------|
| ip-api.com | ۴۵ درخواست/دقیقه |
| ipapi.co | ۱۰۰۰ درخواست/روز |

---

## 📋 لیست کشورهای پشتیبانی

۶۵+ کشور شامل:
🇩🇪 آلمان | 🇳🇱 هلند | 🇺🇸 آمریکا | 🇬🇧 انگلیس | 🇫🇷 فرانسه
🇨🇦 کانادا | 🇦🇺 استرالیا | 🇯🇵 ژاپن | 🇸🇬 سنگاپور | 🇹🇷 ترکیه
و...

---

## 🛠️ توسعه

```bash
# نصب وابستگی‌ها
bun install

# اجرای محلی
bun run dev

# بررسی کد
bun run lint
```

---

## 📄 لایسنس

MIT

---

## 🤝 مشارکت

Pull Request ها خوشایند هستند!

---

**ساخته شده با ❤️ برای جامعه فارسی‌زبان**
