# 🤖 راهنمای کامل راه‌اندازی ربات کانفیگ تلگرام

## 📋 فهرست

1. [پیشنیازها](#پیشنیازها)
2. [راه‌اندازی روی Railway](#راه‌اندازی-روی-railway)
3. [تنظیم متغیرهای محیطی](#تنظیم-متغیرهای-محیطی)
4. [تنظیم Webhook](#تنظیم-webhook)
5. [تنظیم کانال تلگرام](#تنظیم-کانال-تلگرام)
6. [استفاده از ربات](#استفاده-از-ربات)
7. [عیب‌یابی](#عیب‌یابی)

---

## پیشنیازها

### ۱. حساب تلگرام
- یک اکانت تلگرام

### ۲. ایجاد ربات از BotFather
```
1. در تلگرام @BotFather را سرچ کنید
2. /newbot را ارسال کنید
3. نام ربات را وارد کنید (مثلاً: Config Bot)
4. Username ربات را وارد کنید (باید با bot تمام شود)
   مثال: nonecorebot
5. توکن دریافتی را ذخیره کنید
   مثال: 123456789:ABCdefGHIjklMNOpqrSTUvwxYZ
```

### ۳. پیدا کردن ADMIN_ID
```
1. در تلگرام @userinfobot را سرچ کنید
2. /start را ارسال کنید
3. آیدی عددی خود را یادداشت کنید
   مثال: 123456789
```

### ۴. پیدا کردن CHANNEL_ID
```
روش ۱:
1. یک پیام از کانال به @userinfobot فوروارد کنید
2. آیدی عددی کانال نمایش داده می‌شود
   مثال: -1001234567890

روش ۲:
1. به این آدرس بروید (توکن خود را جایگزین کنید):
https://api.telegram.org/botYOUR_TOKEN/getChat?chat_id=@channel_username
```

### ۵. حساب Railway
- ثبت‌نام در [railway.app](https://railway.com)

---

## راه‌اندازی روی Railway

### مرحله ۱: اتصال گیت‌هاب
```
1. وارد Railway شوید
2. New Project را بزنید
3. Deploy from GitHub repo را انتخاب کنید
4. ریپازیتوری پروژه را انتخاب کنید
```

### مرحله ۲: تنظیم متغیرهای محیطی
```
1. پروژه را باز کنید
2. به تب Variables بروید
3. متغیرهای زیر را اضافه کنید:
```

| Variable | Value | توضیحات |
|----------|-------|---------|
| BOT_TOKEN | 123456789:ABC... | توکن ربات |
| ADMIN_ID | 123456789 | آیدی عددی شما |
| CHANNEL_ID | -1001234567890 | آیدی عددی کانال |
| BOT_USERNAME | nonecorebot | یوزرنیم ربات (بدون @) |

### مرحله ۳: دیپلوی
```
1. Deploy را بزنید
2. صبر کنید تا پروژه بیلد شود
3. آدرس پروژه را کپی کنید
   مثال: https://my-app.up.railway.app
```

---

## تنظیم Webhook

### روش ۱: از طریق مرورگر
```
به آدرس زیر بروید:
https://your-app.up.railway.app/api/bot?action=set

پاسخ باید این باشد:
{
  "ok": true,
  "webhook": "https://your-app.up.railway.app/api/bot",
  "result": true
}
```

### روش ۲: از طریق curl
```bash
curl "https://your-app.up.railway.app/api/bot?action=set"
```

### بررسی وضعیت Webhook
```
به آدرس زیر بروید:
https://your-app.up.railway.app/api/bot?action=info
```

---

## تنظیم کانال تلگرام

### مرحله ۱: افزودن ربات به کانال
```
1. وارد کانال شوید
2. روی نام کانال کلیک کنید
3. Subscribers را بزنید
4. Add Subscriber را بزنید
5. یوزرنیم ربات را جستجو کنید
6. ربات را اضافه کنید
```

### مرحله ۲: دادن دسترسی ادمین
```
1. روی ربات در لیست اعضا کلیک کنید
2. Change Permissions را بزنید
3. گزینه‌های زیر را فعال کنید:
   ✅ Post Messages
   ✅ Edit Messages
   ✅ Delete Messages
4. Save را بزنید
```

---

## استفاده از ربات

### کیبورد اصلی

| دکمه | عملکرد |
|------|--------|
| 📤 آپلود HTML | ارسال فایل یا متن حاوی کانفیگ |
| 🔗 لینک ساب | ارسال لینک Subscription |
| 📤 ارسال دستی | ارسال تعداد مشخصی کانفیگ به کانال |
| 📊 آمار | مشاهده آمار ربات |
| ⚙️ تنظیمات | مشاهده تنظیمات فعلی |
| 🗑️ پاک کردن | پاک کردن همه کانفیگ‌ها |

### فرآیند استفاده

```
۱. 📤 آپلود HTML
   ↓
   ارسال فایل HTML یا متن
   ↓
   ربات کانفیگ‌ها را استخراج می‌کند
   ↓
   کشور هر کانفیگ تشخیص داده می‌شود
   ↓
   ریمارک اختصاصی اضافه می‌شود
   ↓
   کانفیگ‌ها در صف قرار می‌گیرند

۲. 📤 ارسال دستی
   ↓
   تعداد کانفیگ را وارد کنید
   ↓
   کانفیگ‌ها به کانال ارسال می‌شوند
```

---

## عیب‌یابی

### ربات پاسخ نمی‌دهد
```
۱. بررسی کنید آیا Webhook تنظیم شده:
   https://your-app.up.railway.app/api/bot?action=info

۲. بررسی کنید متغیرها درست تنظیم شده‌اند

۳. لاگ‌های Railway را بررسی کنید
```

### کانفیگ‌ها به کانال ارسال نمی‌شوند
```
۱. بررسی کنید ربات ادمین کانال است

۲. بررسی کنید دسترسی ارسال پیام دارد

۳. CHANNEL_ID را بررسی کنید (باید با -100 شروع شود)
```

### خطای Unauthorized
```
۱. بررسی کنید ADMIN_ID درست است

۲. بررسی کنید با همان اکانتی پیام می‌فرستید
```

### کانفیگ‌های تکراری
```
ربات به صورت خودکار کانفیگ‌های تکراری را تشخیص می‌دهد
تکراری‌ها در دیتابیس ذخیره نمی‌شوند
```

---

## ساختار فایل‌ها

```
src/
├── app/
│   ├── api/
│   │   └── bot/
│   │       └── route.ts          ← Webhook endpoint
│   └── page.tsx                  ← صفحه وب
│
├── lib/
│   ├── bot/
│   │   └── index.ts              ← ربات تلگرام
│   │
│   ├── parser/
│   │   ├── index.ts              ← پارسر اصلی
│   │   ├── vless.ts              ← پارسر VLESS
│   │   ├── vmess.ts              ← پارسر VMess
│   │   ├── trojan.ts             ← پارسر Trojan
│   │   ├── shadowsocks.ts        ← پارسر SS
│   │   ├── ssr.ts                ← پارسر SSR
│   │   ├── tuic.ts               ← پارسر TUIC
│   │   ├── hysteria2.ts          ← پارسر Hysteria2
│   │   └── types.ts              ← تایپ‌ها و کشورها
│   │
│   ├── utils/
│   │   ├── geoip.ts              ← تشخیص کشور
│   │   └── remark.ts             ← ریمارک و فرمت‌بندی
│   │
│   └── db.ts                     ← دیتابیس JSON
│
└── db/
    └── bot-db.json               ← فایل دیتابیس
```

---

## ویژگی‌ها

### ✅ استخراج کانفیگ
- VLESS, VMess, Trojan, Shadowsocks, SSR, TUIC, Hysteria2
- از HTML، متن، لینک ساب، فایل

### ✅ تشخیص کشور
- از متن پست
- از ریمارک کانفیگ
- از IP سرور با GeoIP API

### ✅ ریمارک خودکار
- فرمت: @BotUsername_COUNTRY
- مثال: @nonecorebot_DE

### ✅ جلوگیری تکرار
- با هش یکتا
- با آدرس IP

### ✅ مدیریت
- کیبورد دو ستونی
- آمار کامل
- پاک کردن دیتابیس

---

## پشتیبانی

برای گزارش مشکل یا پیشنهاد:
- Issue در گیت‌هاب
- تماس با ادمین

---

**نوشته شده با ❤️ برای جامعه فارسی‌زبان**
